name: reserve_go_1

description: A new Flutter project for Reserve & Go

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.4.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.2


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
